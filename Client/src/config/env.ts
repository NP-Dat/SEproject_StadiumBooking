export const config = {
    apiUrl: process.env.HOST || 'http://localhost:5000',
    environment: process.env.NODE_ENV || 'development',
    appName: process.env.REACT_APP_NAME || 'Stadium Booking System',
};
